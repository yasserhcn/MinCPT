priority:
	- build shouldn't be default *CHECK*
	- ability to hide logs *CHECK*
	- fix logs in one line *CHECK*
	- cptmake should build with no arguments *CHECK*
	- add -h option for minCPT *CHECK*

optional:
	- cmake file for building *CHECK*
	- vsc plugin
	- general improvement
	